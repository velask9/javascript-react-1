import React from 'react';
import ReactDOM from 'react-dom/client';

import { Main } from './Student0/Main';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Main />
  </React.StrictMode>
);
